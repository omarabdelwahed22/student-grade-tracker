# API Base URL
# For local development, this can be omitted (will use Vite proxy to localhost:4000)
# For production deployment (e.g., Netlify), set this to your backend API URL
# Example: VITE_API_URL=https://your-backend.example.com/api
VITE_API_URL=
